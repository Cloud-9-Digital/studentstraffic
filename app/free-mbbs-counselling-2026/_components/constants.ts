/** Single source of truth for this landing page's route. Used as the lead
 *  `sourcePath` so CRM/analytics attribution stays consistent across the hero
 *  form, the dialog form and the bottom CTA form. */
export const LP_SOURCE_PATH = "/free-mbbs-counselling-2026";
